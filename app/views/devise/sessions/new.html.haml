%h2 Sign in
= form_for resource, :as => resource_name, :url => session_path(resource_name) do |f|
  - if devise_mapping.rememberable?
    %p
      = f.check_box :remember_me
      = f.label :remember_me
  = f.submit "Log in with WebISO"
  
= render :partial => "devise/shared/links"
